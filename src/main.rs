use rust_aoj::io::value_reader::StdReader;
use rust_aoj::io::value_writer::StdWriter;
use rust_aoj::problems::p_0009::solve;

fn main() {
    let reader = StdReader::new();
    let writer = StdWriter::new();

    solve(reader, writer);
}
